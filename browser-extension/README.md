# Resume Pilot 岗位导入器

1. 打开 Chrome 的 `chrome://extensions/`。
2. 开启“开发者模式”，选择“加载已解压的扩展程序”。
3. 选择本 `browser-extension` 目录。
4. 在 Boss 直聘等网站打开岗位页，完成网站自身的登录/安全验证。
5. 点击扩展图标并选择“读取并复制岗位内容”。
6. 返回 Resume Pilot 的“网站代理获取 JD”，点击“读取剪贴板”后分析。

扩展仅在用户点击时读取当前标签页的可见文本，不读取 Cookie、密码或浏览历史。
